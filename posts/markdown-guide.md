---
title: 지원하는 마크다운 문법
date: 2026-08-25
tags: [개발, 참고]
summary: 이 블로그에서 쓸 수 있는 문법을 한 페이지에 모아 둔 확인용 글입니다.
---

새 스타일을 손볼 때 이 글을 열어두면 무엇이 깨졌는지 한눈에 보입니다.

## 문단과 강조

**굵게**, *기울임*, `인라인 코드`, 그리고 [링크](https://example.com) 를 쓸 수 있습니다.
문단은 빈 줄로 나눕니다. 들여쓰기는 쓰지 않습니다.

## 목록

- 순서 없는 목록
- 두 번째 항목
  - 한 단계 들어간 항목

1. 순서 있는 목록
2. 두 번째
3. 세 번째

## 인용

> 좋은 산문은 유리창과 같다.
> 무엇이 쓰여 있는지 보이되, 유리 자체는 보이지 않는다.

## 코드 블록

```javascript
export function formatDate(iso) {
  const date = new Date(iso);
  if (Number.isNaN(date.getTime())) return String(iso ?? '');
  return new Intl.DateTimeFormat('ko-KR').format(date);
}
```

## 표

| 항목 | 값 | 비고 |
| --- | --- | --- |
| 본문 크기 | 17px | 행간 1.7 |
| 본문 너비 | 68ch | 그 이상 넓히지 않음 |
| 브레이크포인트 | 640 / 960px | 두 개만 사용 |

좁은 화면에서는 표가 본문 대신 스스로 가로 스크롤합니다.

---

## 제목 단계

### 세 번째 단계

#### 네 번째 단계

여기까지가 이 블로그가 다루는 범위입니다.
